# KI-001 Listings

`Listing` в `Autora/Domain/Listing.swift`. Фильтр `ListingFilter.apply`. Сорт `ListingSort`. Цена `PriceConverter`. Bump `BumpPolicy` (20ч). Срок 30 дней без bump → inactive.

Статусы: `active`, `sold`, `draft`, `inactive`.
