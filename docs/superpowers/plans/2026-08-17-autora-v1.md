# Implementation plan — Autora v1

I'm using the writing-plans skill to create the implementation plan.

## Files

- `Autora/Domain/*` — pure logic
- `AutoraTests/*` — TDD
- `Autora/Features/*` — SwiftUI
- `Autora/Services/AppModel.swift`
- `firebase/functions/index.js`
- `firebase/seed/*.json`

## Tasks

1. RED tests PriceConverter, ListingFilter, BumpPolicy, ListingSort
2. GREEN domain
3. seed.json ~40 listings + chats
4. SwiftUI shell + hub + card + favorites + wizard + chat + profile
5. Functions seed/wipe snippets
6. `xcodebuild test` then build
